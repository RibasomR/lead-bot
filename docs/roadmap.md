# 🗺️ Roadmap LeadHunter — Пошаговый план разработки

## Легенда
- ⏳ В процессе
- ✅ Выполнено
- ❌ Отменено
- 🔄 Требует доработки

---

## Фаза 0: Подготовка инфраструктуры

### 0.1. Инициализация проекта
- [x] Создать структуру папок проекта
- [x] Создать `.gitignore` для Python/Docker
- [x] Создать `requirements.txt` для зависимостей
- [x] Создать `.env.example` с шаблоном переменных окружения
- [x] Создать `README.md` с описанием проекта и инструкциями запуска

### 0.2. Настройка конфигурации
- [x] Создать модуль `config.py` для загрузки переменных окружения
- [x] Определить структуру конфигурационных параметров
- [x] Настроить валидацию переменных окружения при старте

---

## Фаза 1: База данных PostgreSQL

### 1.1. Проектирование схемы БД
- [x] Создать SQL-миграции для таблицы `accounts`
- [x] Создать SQL-миграции для таблицы `chats`
- [x] Создать SQL-миграции для таблицы `leads`
- [x] Создать SQL-миграции для таблицы `lead_ai_data`
- [x] Создать SQL-миграции для таблицы `replies`
- [x] Добавить индексы для оптимизации запросов
- [x] Добавить внешние ключи и ограничения целостности

### 1.2. Подключение к БД
- [x] Создать модуль `database.py` для работы с PostgreSQL
- [x] Настроить пул соединений с БД (asyncpg или SQLAlchemy)
- [x] Создать функции инициализации БД и применения миграций
- [x] Реализовать базовые CRUD-операции для всех таблиц

### 1.3. Docker для PostgreSQL
- [x] Создать `Dockerfile` для PostgreSQL (или использовать официальный образ)
- [x] Настроить volume для персистентности данных
- [x] Добавить сервис `db` в `docker-compose.yml`

---

## Фаза 2: AI Advisor (интеграция с OpenRouter)

### 2.1. HTTP-клиент для OpenRouter
- [x] Создать модуль `ai_advisor.py`
- [x] Реализовать базовый HTTP-клиент (httpx/aiohttp) для OpenRouter API
- [x] Настроить обработку ошибок и таймаутов запросов
- [x] Добавить поддержку двух моделей (primary и secondary)

### 2.2. Функции генерации контента
- [x] Реализовать функцию `generate_lead_summary()` — краткое описание лида
- [x] Реализовать функцию `suggest_reply_options()` — варианты ответов для разных стилей
- [x] Реализовать функцию `score_lead()` — оценка качества лида и вилка цен
- [x] Настроить промпты для каждой функции с учётом стилей (вежливый/дружеский/жёсткий)

### 2.3. Кеширование ответов ИИ
- [x] Реализовать сохранение ответов ИИ в таблицу `lead_ai_data`
- [x] Реализовать проверку наличия кеша перед новым запросом к API
- [x] Добавить возможность принудительного обновления ответов ИИ

---

## Фаза 3: Admin Bot (aiogram)

### 3.1. Базовая структура бота
- [x] Создать модуль `admin_bot/main.py`
- [x] Настроить инициализацию aiogram 3.x (Bot, Dispatcher)
- [x] Реализовать фильтр доступа по `user_id` оператора
- [x] Создать основное меню с командами `/start`, `/help`

### 3.2. Управление чатами
- [x] Реализовать команду `/add_chat` — добавление чата в мониторинг
- [x] Реализовать команду `/list_chats` — просмотр списка чатов
- [x] Реализовать команду `/enable_chat` / `/disable_chat` — вкл/выкл мониторинга
- [x] Добавить функционал белого/чёрного списка чатов

### 3.3. Управление аккаунтами
- [x] Реализовать команду `/add_account` — регистрация Telegram-аккаунта
- [x] Реализовать команду `/list_accounts` — просмотр списка аккаунтов
- [x] Реализовать привязку стиля к аккаунту (вежливый/дружеский/жёсткий)

### 3.4. Отображение лид-карточек
- [x] Создать модуль `admin_bot/lead_card.py` для формирования карточек
- [x] Реализовать функцию генерации текста лид-карточки с:
  - Заголовком (из AI)
  - Текстом сообщения заказчика
  - Информацией о чате и ссылкой на сообщение
  - Рекомендациями от ИИ (оценка, тон, цены)
- [x] Создать inline-клавиатуру с кнопками:
  - "Акк1: Вежливо", "Акк2: Нейтрально", "Акк3: Жёстко"
  - "Предложить тексты"
  - "Быстрая отправка"
  - "Свой текст"
  - "Следующий лид", "Предыдущий лид"

### 3.5. Обработка действий оператора
- [x] Реализовать callback-хендлеры для выбора аккаунта и стиля
- [x] Реализовать обработчик кнопки "Предложить тексты" (запрос к AI Advisor)
- [x] Реализовать обработчик кнопки "Быстрая отправка"
- [x] Реализовать обработчик кнопки "Свой текст" (ожидание текста от пользователя)
- [x] Реализовать отправку сообщения через Lead Listener API

### 3.6. История лидов и навигация
- [x] Реализовать команду `/leads` — просмотр истории лидов
- [x] Добавить пагинацию (по 5-10 лидов на страницу)
- [x] Реализовать фильтры по времени (сегодня/3 дня/неделя/месяц)
- [x] Реализовать быстрый переход к лиду по ID
- [x] Реализовать навигацию "Следующий/Предыдущий лид"

### 3.7. Обновление статусов лидов
- [x] Автоматическое изменение статуса лида на `viewed` при открытии карточки
- [x] Изменение статуса на `replied` после отправки ответа
- [x] Возможность пометить лид как `ignored`

### 3.8. Docker для Admin Bot
- [x] Создать `Dockerfile` для Admin Bot
- [x] Добавить сервис `admin_bot` в `docker-compose.yml`
- [x] Настроить зависимость от сервиса `db`

---

## Фаза 4: Lead Listener (userbot-сервис)

### 4.1. Базовая структура userbot-сервиса
- [x] Создать модуль `lead_listener/main.py`
- [x] Выбрать библиотеку (Telethon или Pyrogram)
- [x] Создать систему управления несколькими клиентами (до 3-5 аккаунтов)

### 4.2. Авторизация userbot-аккаунтов
- [x] Реализовать CLI-утилиту для логина аккаунтов (ввод кода/2FA)
- [x] Сохранять сессии в безопасном месте (например, папка `/sessions`)
- [x] Реализовать автоматическое переподключение при разрыве связи

### 4.3. Подписка на чаты и мониторинг
- [x] Реализовать загрузку списка чатов из БД при старте
- [x] Подписаться на события новых сообщений во всех активных чатах
- [x] Реализовать распределение чатов между несколькими аккаунтами

### 4.4. Фильтрация и определение лидов
- [x] Создать модуль `lead_listener/filters.py`
- [x] Реализовать загрузку ключевых слов из конфига/БД
- [x] Реализовать фильтр по ключевым словам (регистронезависимый, мультиязычный)
- [x] Реализовать определение языка сообщения (ru/en)
- [x] Реализовать извлечение стека технологий из текста
- [x] Реализовать проверку на дубликаты (по `chat_id` + `message_id`)
- [x] Игнорировать сообщения от ботов и системные сообщения

### 4.5. Создание лидов и отправка в Admin Bot
- [x] При срабатывании фильтра создавать запись в таблице `leads`
- [x] Извлекать информацию об авторе сообщения
- [x] Формировать прямую ссылку на сообщение в чате
- [x] Отправлять уведомление в Admin Bot (через HTTP API или напрямую)
- [x] Инициировать запрос к AI Advisor для анализа лида

### 4.6. Отправка ответов от имени userbot
- [x] Создать HTTP API-эндпоинт для приёма команд отправки
- [x] Реализовать функцию отправки сообщения в чат от выбранного аккаунта
- [x] Добавить случайную задержку перед отправкой (антиспам)
- [x] Логировать отправленные сообщения в таблицу `replies`

### 4.7. Антиспам и лимиты активности
- [x] Реализовать счётчик откликов в час на один чат
- [x] Добавить проверку лимитов перед отправкой
- [x] Реализовать случайные задержки между действиями (2-10 секунд)
- [x] Логировать все попытки отправки (успешные и заблокированные)

### 4.8. Docker для Lead Listener
- [x] Создать `Dockerfile` для Lead Listener
- [x] Добавить сервис `lead_listener` в `docker-compose.yml`
- [x] Настроить зависимость от сервиса `db`
- [x] Настроить volume для хранения сессий userbot

---

## Фаза 5: Интеграция компонентов

### 5.1. API между сервисами
- [x] Определить формат взаимодействия (HTTP REST API или shared database)
- [x] Реализовать API Lead Listener для отправки сообщений из Admin Bot
- [x] Реализовать отправку новых лидов из Lead Listener в Admin Bot

### 5.2. Обработка ошибок и логирование
- [x] Настроить логирование для всех сервисов (уровни DEBUG, INFO, WARNING, ERROR)
- [x] Реализовать обработку ошибок при недоступности БД
- [x] Реализовать обработку ошибок при недоступности OpenRouter API
- [x] Реализовать fallback на вторую модель AI при сбое первой
- [x] Добавить уведомления оператору о критических ошибках

### 5.3. Финальная настройка Docker Compose
- [x] Объединить все сервисы в единый `docker-compose.yml`
- [x] Настроить общую сеть для контейнеров
- [x] Настроить переменные окружения через `.env`
- [x] Настроить политику перезапуска `restart: always`
- [x] Настроить healthcheck для всех сервисов

---

## Фаза 6: Финализация MVP

### 6.1. Тестирование полного цикла
- [ ] Протестировать добавление чатов через Admin Bot
- [ ] Протестировать авторизацию userbot-аккаунтов
- [ ] Протестировать обнаружение лида по ключевым словам
- [ ] Протестировать генерацию лид-карточки с рекомендациями AI
- [ ] Протестировать отправку ответа через userbot
- [ ] Протестировать работу истории лидов и навигации

### 6.2. Оптимизация и багфиксинг
- [x] Исправить обнаруженные баги
- [x] Оптимизировать SQL-запросы (добавить индексы при необходимости)
- [x] Оптимизировать работу с API OpenRouter (батчинг, кеширование)
- [x] Провести нагрузочное тестирование на 30+ чатах

### 6.3. Документация
- [x] Актуализировать `README.md` с финальными инструкциями
- [x] Создать документацию по настройке `.env`
- [x] Создать документацию по первому запуску и авторизации аккаунтов
- [x] Документировать API между сервисами (если применимо)
- [x] Создать troubleshooting-гайд для частых проблем

---

## Фаза 7: Автопоиск и рекомендации каналов

### 7.1. Интеграция с каталогами и поиском
- [x] Определить внешние источники данных для поиска каналов (Telegram Search + опционально TGStat)
- [x] Реализовать сервис/модуль `shared/channel_discovery/` для поиска каналов по ключевым словам (боты, Python, фриланс, финтех, крипта и т.д.)
- [x] Создать миграцию БД для таблицы `channel_candidates` (id, ссылка, название, описание, метрики, источник)
- [x] Реализовать модель `ChannelCandidate` в `models.py`
- [x] Добавить CRUD операции для работы с кандидатами каналов
- [x] Реализовать провайдер `TelegramSearchProvider` для поиска через Telethon
- [x] Реализовать опциональный провайдер `TGStatProvider` для обогащения данных
- [x] Создать основной сервис `ChannelDiscoveryService` для координации поиска
- [x] Добавить параметры конфигурации в `config.py` и `env.example` (CHANNEL_SEARCH_KEYWORDS, CHANNEL_POSTS_COUNT, TGSTAT_API_KEY)
- [x] Реализовать сбор N последних постов из каналов для AI-анализа

### 7.2. AI-оценка и отбор кандидатов
- [x] Реализовать сбор дополнительного контента для оценки: получать не только название и описание канала, но и N последних постов (N настраивается в конфиге, например 3–10), чтобы лучше понимать реальный контекст и тип публикуемых заказов
- [x] Реализовать функцию анализа описаний и последних постов канала через AI Advisor, чтобы оценивать релевантность под целевой профиль (боты, автоматизация, разработка, финансы/крипта)
- [x] Определить формат оценки: score релевантности (0-10), краткий комментарий, предполагаемый тип заказов (фриланс, вакансии, стажировки и т.п.), confidence level
- [x] Реализовать ранжирование каналов по итоговому скору и отсечку по минимальному порогу качества
- [x] Добавить метод `evaluate_channel()` в AI Advisor с детальным промптом
- [x] Интегрировать AI-оценку в `ChannelDiscoveryService`
- [x] Реализовать автоматическую оценку после поиска каналов
- [x] Добавить метод `get_top_rated_candidates()` для получения лучших кандидатов
- [x] Обновить тестовый скрипт для демонстрации AI-оценки

### 7.3. UX в Admin Bot: поиск и добавление
- [x] Добавить в Admin Bot новую команду/кнопку "🔍 Найти каналы" для запуска поиска и AI-оценки каналов
- [x] Реализовать отображение списка рекомендованных каналов в виде карточек (название, ссылка, краткий комментарий AI, примерный тип заказов)
- [x] Добавить кнопку "➕ Добавить лучшие" для массового добавления отобранных каналов в whitelist (`chats.enabled = true`, `is_whitelisted = true`)
- [x] Добавить возможность точечного управления: у каждой карточки отдельные кнопки "✅ Добавить в мониторинг" / "🚫 Игнорировать"
- [x] Создать новый хендлер `channel_discovery.py` с обработчиками
- [x] Добавить клавиатуры для навигации по кандидатам
- [x] Реализовать API endpoint `/api/discover_channels` в Lead Listener
- [x] Интегрировать с существующим меню чатов

---

## Фаза 8: Управление ключевыми словами через бот

### 8.1. UX для редактирования ключевых слов
- [ ] Добавить раздел "⚙️ Настройки" → "🔤 Ключевые слова"
- [ ] Реализовать просмотр текущих ключевых слов для поиска лидов (LEAD_KEYWORDS)
- [ ] Реализовать просмотр текущих ключевых слов для поиска каналов (CHANNEL_SEARCH_KEYWORDS)
- [ ] Добавить кнопки "➕ Добавить слово" и "➖ Удалить слово"
- [ ] Реализовать FSM состояния для ввода новых ключевых слов
- [ ] Добавить валидацию (минимум 2 символа, без спецсимволов, дедупликация)

### 8.2. Бэкенд для управления ключевыми словами
- [ ] Создать CRUD операции для динамического обновления keywords в памяти
- [ ] Реализовать сохранение изменений в конфигурационный файл или отдельную таблицу БД
- [ ] Добавить hot-reload конфигурации без перезапуска контейнеров
- [ ] Реализовать экспорт/импорт списка ключевых слов (JSON/CSV)
- [ ] Добавить историю изменений ключевых слов с timestamp

### 8.3. Дополнительные фичи
- [ ] Группировка ключевых слов по категориям (Боты, Веб, Финтех, Крипта и т.д.)
- [ ] Статистика по ключевым словам (сколько лидов/каналов нашлось по каждому слову)
- [ ] Умные подсказки от AI: предложить похожие/релевантные ключевые слова
- [ ] Быстрые пресеты: "Только боты", "Только веб", "Всё IT" и т.д.

---


